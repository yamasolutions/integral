%ul.list-group.list-group-horizontal
  - if @resource.respond_to?(:subscribable?) && @resource.subscribable?
    %li.list-group-item= render partial: 'integral/backend/shared/notification_subscription_toggle', locals: { subscribable: @resource }
