/**
 *  Media Upload is handled through the media gallery - This is only used as a stub so that media upload buttons are displayed
 */
export async function uploadMedia( {
	allowedTypes,
	additionalData = {},
	filesList,
	maxUploadFileSize,
	onError = noop,
	onFileChange,
	wpAllowedMimeTypes = null,
} ) {
}
