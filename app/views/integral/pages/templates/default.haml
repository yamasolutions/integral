-# Uncomment this to easily update demo markup
-# = render partial: 'integral/pages/demo'

= link_to t('integral.actions.edit'), edit_backend_page_url(@resource.id), class: 'button primary button--edit-resource', data: { 'turbo' => 'false' } if current_user.present?
.wysiwyg-content
  = @resource.render_active_block_list
