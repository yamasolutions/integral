require 'rails_helper'

module Integral
  describe "Activities CRUD", :type => :feature do

    before do
      # sign_in(create(:page_manager))
      # click_on 'Pages'
    end

    xit "can view the activities page" do
      # sleep 1
      # all('tbody tr.odd a')[0].trigger 'click'
      #
      # expect(page).to have_content page.title
    end
  end
end
