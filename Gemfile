source 'https://rubygems.org'

ruby '2.5.8'

# Gem dependencies in integral.gemspec.
gemspec

# TEMP: Remove this when > 0.5.0 is released
group :test do
  gem 'apparition', git: 'git://github.com/twalpole/apparition.git'
end

