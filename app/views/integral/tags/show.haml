= render layout: 'integral/shared/blog/layout', locals: { title: @meta_data[:page_title] } do
  = render partial: @tagged_posts, cached: true
  .center= will_paginate @tagged_posts, renderer: Integral::BootstrapPaginationRenderer
