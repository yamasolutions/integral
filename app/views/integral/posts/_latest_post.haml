- cache post do
  .latest-post
    = link_to integral.post_url(post.slug) do
      = image_tag post.image_url(size: :large), alt: post.title
    %h2.latest-post-title= link_to post.title, integral.post_path(post.slug)
    %p= post.description
    %ul.list-unstyled.piped-list
      %li= post.published_at
      %li= link_to post.category.title, category_path(post.category)
